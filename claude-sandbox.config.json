{
  "dockerfile": "./claude-sandbox.Dockerfile",
  "setupCommands": [
    "sudo socat UNIX-LISTEN:/tmp/ssh-agent.sock,fork,mode=0666 TCP:host.docker.internal:7777 2>/dev/null &",
    "git config --global --unset gpg.ssh.program"
  ]
}
